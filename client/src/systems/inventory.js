/**
 * INVENTORY SYSTEM (client) — empty slot.
 *
 * Pairs with server/systems/inventory.js. It only runs once that server
 * system is enabled. See CLAUDE.md for the contract.
 */

export default {
  id: 'inventory',

  handlers: {},
}
