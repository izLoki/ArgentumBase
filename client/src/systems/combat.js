/**
 * COMBAT SYSTEM (client) — empty slot.
 *
 * Pairs with server/systems/combat.js. It only runs once that server system
 * is enabled. See CLAUDE.md for the contract.
 */

export default {
  id: 'combat',

  handlers: {},
}
