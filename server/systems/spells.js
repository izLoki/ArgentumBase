/**
 * SPELLS SYSTEM — empty slot.
 *
 * Nothing is implemented here on purpose. Flip `enabled` to true and build it.
 * The contract and the conflict-free workflow are documented in CLAUDE.md.
 */

export default {
  id: 'spells',
  enabled: false,

  handlers: {},
}
