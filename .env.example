# Where the browser should reach the game server.
# Only needed when the client and the server are deployed separately.
# Read at BUILD time by Vite, so rebuild after changing it.
VITE_SERVER_URL=https://your-server-host.example.com

# Server listening port (server side only, defaults to 3000).
# PORT=3000
